/** @jsx React.DOM */

var SearchBar = {};

SearchBar.render = function() {
  return (
    <div className="search-bar">
      Search Bar
    </div>
  );  
};

module.exports = React.createClass(SearchBar);